In order to view the full projects navigate to the branch by the project name.

The HTML documents for each project will be on the main branch.

The trio of HTML CSS and JavaScript documents will be found on their named branches (ex.: catTracker.html is the HTML on the main branch and the catTracker branch has all the documents inside)
